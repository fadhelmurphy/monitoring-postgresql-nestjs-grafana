apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: postgres
    password: password
    database: monitoring
    isDefault: true
    editable: true
    jsonData:
      sslmode: "disable"
